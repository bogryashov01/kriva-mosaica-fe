{
  "installCommand": "yarn install --ignore-optional",
  "buildCommand": "yarn run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
